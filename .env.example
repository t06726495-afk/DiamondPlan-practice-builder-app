DATABASE_URL="file:./dev.db"

# Optional, enables the "Ask AI Coach" chat feature. Without it, the app
# still works fully; the AI chat just shows a "not configured" message.
# Free API key at https://console.groq.com/keys
GROQ_API_KEY=
